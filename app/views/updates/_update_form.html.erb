<%= form_for([@issue, @update], :html => {:class => 'form-horizontal'}) do |f| %>
  <%= render 'layouts/errors', object: f.object %>
  <div class="form-group">
    <%= f.label :name, class: 'col-md-2 label-control' %>
    <div class="col-md-10">
      <%= f.text_field :name, class: 'form-control' %>
    </div>
  </div>

<% end %>
